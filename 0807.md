# 数据库 - 视图 函数 存储过程 C API

## 回顾
- 关系范式:1NF 2NF 3NF
- 表的关系
    - 一对一:通过共享主键建立
    - 一对N
    - N对N:通过中间表
- 索引
    - 类型:主键索引,唯一索引,全文索引,默认索引,空间索引,位图索引,低基数列索引
    - 技术实现:B+Tree，hash表，R树
    - 主键,唯一约束,外键,where,order by,group by
## 视图

是虚表，存储在数据库的sql

表:数据的存储结构
视图:数据的逻辑结构，业务结构

视图的数据是否可以插入,更新和删除:简单视图（单表、无聚合、无计算列）通常可以直接更新；复杂视图一般不允许直接修改。

```sql
create view v_dept 
as
select s.id,d.name,count(s.id) n
from dept d
left join staff s
on d.id=s.dept_id
group by d.id d.name
order by d.id;

select * from v_dept;
```

## 函数

- 内置函数
- 自定义函数

设置权限
set global log_bin_trust_function_createtos=true;

```sql
CREATE FUNCTION `f1` (sal int)
RETURNS INTEGER
BEGIN

RETURN 1;
END
```

## 存储过程,触发器

存储过程

## SQL优化

##MySql C API

核心数据(结构体)
- MySql             数据库连接
- MySql_row         从结果集获得的一行数据，记录，字符串数组
- MySql_res         执行SQL获得资源，结果集
- MySql_field       列，字段的元数据：列名，类型，大小

函数
- mysql_init()
- mysql_real_connect()
- mysql_close()
- mysql_query()
- mysql_store()_result()
- mysql_fetch_field()
- mysql_feych_row()
- mysql_close_res()

## C API 库
sudo apt install libmysqlclient-dev

cd /user/include

## 如何避免SQL注入

MYSQL_STMT



优点:
1.语句在处理数据库引擎中提前编译好，每次调用直接执行
2.SQL语句不进行字符串拼接,避免了SQL注入

```c
//预编译语句
char *sql = "select id,name,dept from dept where id=?";
char *sql = "insert into dept(id,name) values('?','?')";

```

