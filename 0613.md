# pthread 线程

## 线程的生命周期
- 新建  pthread_create()
- 就绪  OS 调度，获得cpu的时间片
- 运行  1.结束 2.就绪(时间片用完了，任务还没结束) 3.阻塞
- 阻塞(sleep,wait)  就绪
- 结束

## 分离态和非分离态
- join 非分离态(默认)，pthread_join()，若不join会出现僵死线程
- detach 分离态,子线程与主线程分离，不可join，不返回数据，任务结束自动释放资源

## 非分离态

int main()
{
    创建子线程
    pthread_create(t1);

    code 1;
    //pthread_join(t1)//需要等子线程t1结束，并获得返回值
    code 2;
    code 3;

    pthread_exit(NULL);//结束当前（主）线程，子线程t1可以继续进行，但没有join，进程未结束前t1成为僵死线程
    return 0;//进程结束，所有资源销毁，包括t1
}