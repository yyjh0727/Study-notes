# 数据库

# TCL 事务控制语句
- BEGIN         开启手动事务模式，知道提交
- COMMIT
- ROLLBACK
- 

## 回顾
- SQL：DDL、DML、TCL
- 数据对象：表、索引、视图、过程、函数、触发器、约束
- DDL：数据定义语言，CREATE,ALTER,DROP,GRANT
- DML：CRUD 增删改查，INSERT,SELECT,UPDATE,DELETE
- 表连接
    - 内连接：交集
    - 左外连接：
    - 右外连接：
    - 全外连接：并集
操作
    - 交集
    - 并集
    - 差集

```sql
-- 并集 union
select id,val from t1
union
select id,val from t2;

-- 交集
select t1.val from t1,t2 where t1.val=t2.val;
select t1.* from t1 inner join t2 on t1.id=t2.id;

-- 差
select t1.* from t1 left join t2 on t1.id=t2.id where t2.id IS NULL;

```

```sql
SELECT
    c1,c1
FROM
    t1
JOIN
    t2
ON
    t1 = t2
WHERE 过滤条件
GROUP BY 分组/聚合函数
HAVING 过滤分组数据
ORDER BY 排序
LIMIT 分页

## 事务

事务:一个业务操作包含多个数据操作DML 这多个数据操作在逻辑上是一个整体，它的执行或者撤销被称为一个事务
特性(ACID)
- 原子性:事务是一个不可分割的最小工作单元，事务中的所有操作要么全部成功执行，要么全部不执行。
- 一致性:事务执行前后，数据库必须从一个一致状态转换到另一个一致状态。所有数据必须满足预定义的完整性约束（如外键、唯一性、检查约束等）。
- 隔离性:多个事务并发执行时，一个事务的执行不应被其他事务干扰。每个事务都感觉不到其他事务在并发执行
    -隔离级别
        - 读未提交:安全级别最低，存在脏读，不可重复读，幻读的问题
        - 读已提交:存在不可重复度，幻读的问题
        - 可重复读:存在幻读的问题
        - 串行化:安全级别最高，三个问题全防住
- 持久性:一旦事务通过 COMMIT 提交，其所做的修改就是永久性的，即使系统随后发生崩溃（如断电、宕机），数据也不会丢失。

```sql
 --转账成功
BEGIN;

update account set balance=balance-5 where id=1;//给一号扣五块钱
update account set balance=balance+5 where id=2;

insert into log(`from`,`to`,amount) values(1,2,5);//添加一个日志

//ROLLBAKE;撤销前面的操作

COMMIT;

```

## 索引 index

目标:提高查询检索速度，负面:插入，删除，更新

具体实现:B+Tree，Hash

自动创建索引:主键列，外键列，唯一约束
手动创建索引


## 视图