# 数据库

## SQL 优化
- 索引:B+tree,hash
    - 自动:主键，外键，唯一约束
    - 手动:where,join,order by,group by
    - 避免了全表扫描，回表问题
    - 影响插入，更新，删除的性能
- 优化SQL语句
    - 禁用 SELECT*
    - 避免子查询和临时表(基于执行计划再次优化)
    - join 小表()驱动大表
    - 批量的写
- 执行计划 explain
    - explain SELECT
- 缓存(*)
    
- 分表
    - 水平拆分
        - 表分区:数值范围，值 list，hash
        - 水平分表:create table t1_p1 as select
    - 垂直拆分
- 分库
    - 读写分离:主从架构，从主库写，

## Redis

功能
- 缓存服务
- 数据结构实现

数据类型
- string
- list
- set
- sorted set
- hash