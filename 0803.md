# 数据库

## 数据库

数据库管理系统(数据库服务器):DBMS 是一个软件，核心功能是创建数据库，操作数据库，数据库的备份与恢复

数据库:磁盘上的一组文件，包括多个数据文件，日志文件，控制文件等

数据库实例:数据库服务器加载数据库文件创进程

如用wps(DBMS)创建了一个word文件(数据库)

## 数据库类型
- 关系数据库
    - MySQL
    - Oracle
    - Db2
    - PostgreSQL
    - SQLite
- 对象数据库
- NoSQL
    - 文档数据库
    - 键值数据库
    - 行数据库
    - 图数据库
    - 时序数据库

## MySQL

安装:sudo apt install mysql-server

状态:service mysql status

端口:sudo netstat -tpnl | grep mysql

启动/关闭:sudo service mysql start
        sudo service mysql stop

# 链接
- mysql客户端
- mysql GUI 图形化工作台
- navcat 第三方

sudo mysql

# 核心操作
show databases;     查看数据库
create database abc;添加数据库abc
drop database abc;  删除数据库
use abc;            连接数据库
show tables;        显示表
desc user;          查看表的结构
select * from user; 查询表中的数据
select * from user\g查询表中的数据
select host,user from user\g查询表中的数据,指定列的名称

## SQL结构化查询语言

一套技术规范 标准化SQL

### DDL 数据定义语言
- CREATE        创建
- DROP          删除
- ALTER         修改
- TRUNCATE      截断

### DML 数据操作语句
- INSERT        增
- DELETE        删
- UPDATE        改
- SELECT        查

### TCL 事务控制语句
- BEGIN         开始
- COMMIT        提交
- ROLLBAKC      回滚

sql数据库基础代码
create table person//添加表
(
    name varchar(16) NOT NULL,//可加NOT NULL进行限制，varchar代表可变字符
    age int default(18),//default代表默认值
    gender enum('',''),
    tel char(11)
);

insert person values('bob',21,'1','18600000000');
insert person values('bob',21,'1','18600000000'),('',21);//一次插入两行数据
insert person(name) values('bob');//只插入名字


## 数据类型
- 数值
    - bit           1
    - tinyint       2
    - smallint      4
    - int/integer   8       number
    - float
    - double
    - decimal(7,2)
- 日期与时间
    - date
    - time
    - datetime
    - timetamp
    - year
- 字符串
    - char 255 长度固定 快
    - varchar 65535 长度不固定，空间:N+1
    - text/tinytext 文本数据
    - binary/blob 二进制数据，不存图片和多媒体
    - enum 枚举
    - set 集合
- 空间
    - 点
    - 线
    - 面
- JSON 数据交换格式
    - {} 对象
    - [] 数组或列表
...js
    {
        "x":2,
        "y":"hello",
        "z":true,
        "n":[],
        "m":{}
    }
...
create table t1
(
    c1 date,
    c2 time,
    c3 datetime,
    c4 timetamp,
    c5 year
)

insert t1 values(now(),now(),now(),now(),now());

select 2+3 from dual;
select sysdate(),now(),curtime(),curdate() from dual;

select adddate(curdate(),30);//当前日期加30天
select datediff("","")//两个时间节点的差值

函数
- sysdate()
- systime()
- time()

create table t2
(
    c1 enum("男","女","保密"),
    c2 set("A","B","C","D",)
);

insert t2 values("男","ABC");
insert t2 values(1,3),values(2,7),values(1,9);

文档数据库 MangoDB，JSON/BSON

create table t3
(
    c1 JSON
);

insert t3 values('{x:1}');
insert t3 values('{"x":2,"y":200}');
insert t3 values();

## 关系建模:考虑每个字段的关系

表存储
- 实体或者概念的特征或属性，表由多个字段，多个行构成
- 实体与实体之间的关系

关系建模 - 关系代数

关系范式:设计表的一套指导思想
- 1NF:列的数据原子性，不可再分
- 2NF:创建复合主键，主键是不重复的，唯一的，所有字段都要直接依赖于主键字段，没有依赖的拿出去
- 3NF:非键字段之间不存在依赖关系

```sql
//PK:主题约束
//FK:外键:外键字段是另一张表或者另一个记录的主键值
    create table t5
    (
        id int primary key,
        name char(16),
        gender enum(),
        birthday data,
        tel char(11),
        mom char(16)
    );
```

