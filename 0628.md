# 线程同步

## 互斥锁
单核，多核场景都适用,cpu占用低，适用临界区比较大的场景

## 条件变量

## 信号量
原子性的整数，进行PV操作

参数:
1.信号量变量
2.0表示进程内  1表示进程间
3.信号量的值:1表示二元信号量，其他表示计数信号量

sem_init(&sem,0,1)

P:获得资源 sem_wait()
    - sem>0,获得资源，sem--
    - sem=0,进入阻塞状态
V:释放资源 sem_post()
    - sem++

## 自旋锁 spin_lock

与互斥锁都是排他锁,资源加锁后只能被一个线程独占使用，直到解锁
自旋锁不适合单核场景，为多核场景而生，不断轮回访问，以获得锁,避免了上下文切换时间，cpu占用高
    适用临界区比较小的场景

死锁:多个线程竞争资源时，出现的问题

## 读写锁 


## 任务
1.代码统计
    - 定义线程池 16
    - 任务队列 80000
    - 异步与同步
    - 锁
    - 写入文件

2电梯

### 回顾
B树 把数据分散在每个节点，适合单点精确查找；
B+树 把数据集中在叶子节点，非叶子节点只存索引，适合磁盘存储和范围查询。