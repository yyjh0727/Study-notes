# 套接字编程 - TCP

## 套接字 socket

套接字是 TCP/IP 实现中出现的概念，封装网络底层通信细节，操作方式类似于文件

### IP地址
接入网络的计算机的逻辑标识。动态分配给计算机的一个标识信息

物理标识:硬件设备的标识，出厂设定，唯一标识
MAC地址

逻辑标识
IPv4:2^32    42亿
IPv6:2^128  

### Port端口号

一台计算机有唯一的IP标识，但同时运行上百个进程，端口号标识收发数据的进程
端口号也是虚拟的，2^16,65536个，前1014保留

## TCP套接字API
- socket()      创建套接字文件描述符 int
- bind()        绑定地址
- listen()      开启监听，初始化一个创建连接的队列
- accept()      接收请求
- read/write    读写操作
- close()       关闭套接字

## 任务
1.实现一个客户端的多次读写（循环加条件）

2.实现可以同时连接多个客服端（多线程）