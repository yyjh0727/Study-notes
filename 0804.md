# 数据库 - SQL

## DDL 数据定义语言
- `CREATE`      创建
- `DROP`        删除
- `ALTER`       修改
- `TRUNCATE`    截断

```sql
    create database udb;
    use udb;
    show tables;

    create table t1
    (
        id int auto_increment primary key,
        c2 char(4),
        c3 decimal(7,2)
    );

    desc t1;展示表t1结构
    SELECT * FROM t1;查看表里的数据

    ALTER TABLE t1 ADD c4 varchar(16);往表里添加c4
    ALTER TABLE t1 CHANGE c2 title varchar(16);把c2的名字改为title
    ALTER TABLE t1 DROP COLUMN c3;删除c3

    INSERT INTO t1 (title) VALUES (abcd);插入数据
****
    TRUNCATE TABLE t1;相当于先后使用了drop,create；保留了表的结构，删除表的数据，释放了空间，就相当于重置了表

    DROP TABLE t1;删除表的所有东西

    DELETE TABLE t1;
    DELETE TABLE t1 WHERE XXX;若不加 WHERE 则删除全部数据，否者定向删除
****
    --GRANT 授予权限
    scp '目录' yjh@192.168.239.128:/home/yjh  把windows的文件传到Linux中

    source '数据库在Linux的路径'; 文件里的所有 SQL 语句逐条导入并执行到当前数据库中

    CREATE USER 'abc'@'%' identified BY '123456';
    
    GRANT ALL ON udb.* TO 'abc'@'%';把数据库 udb的所有权限授予给用户abc，% 是指可以从任意地方连接

    mysql -u 用户名 -p密码 使用客户端连接

启用远程连接
    cd mysql.conf.d
    sudo nano mysqld.cnf
    进入控制器将bind 改成0.0.0.0,保存退出
    sudo service mysql restart



```
## DML 数据操作语句 CRUD CreateReadUpdateDelete 
- `INSERT`      增
- `DELETE`      删
- `UPDATE`      改
- `SELECT`      查
- ``

## TCL 事务控制语句
- `BEGIN`       开始
- `COMMIT`      提交
- `ROLLBACK`    回滚
- ``
