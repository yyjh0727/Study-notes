# 套接字编程 - UDP

## TCP&UDP
|特征|TCP|UDP|
|   |   |   |
|建立连接|是|否|
|是否有序|是|否|
|丢失之后|丢包重发|丢失|
|是否可靠|是|否|
|数据大小|不受限|理论最大值65535，MTU=1500-28|

| 协议      | 常见应用场景                | 原因              |
| ------- | --------------------- | --------------- |
| **TCP** | 网页浏览 (HTTP/HTTPS)     | 需要确保页面内容完整、正确加载 |
|         | 文件传输 (FTP/SFTP)       | 数据不能丢失或损坏       |
|         | 电子邮件 (SMTP/POP3/IMAP) | 邮件内容必须完整送达      |
|         | 远程登录 (SSH/Telnet)     | 指令必须按序、可靠执行     |
|         | 数据库访问                 | 事务数据完整性要求高      |
| **UDP** | 在线视频/直播 (流媒体)         | 实时性优先，少量丢包可接受   |
|         | 语音通话 (VoIP)           | 低延迟比完美恢复更重要     |
|         | 在线游戏                  | 实时状态同步，延迟敏感     |
|         | DNS 查询                | 请求小、响应快，超时可重发   |
|         | 网络广播/组播               | 一对多高效传输         |
|         | IoT 传感器上报             | 高频小数据，降低开销      |

TCP 服务器开发流程
- socket
- setsockopt
- bind
- listen
- accept
- r/w
- close

TCP 客户端开发流程
- socket
- connect->accept
- r/w
- close

## UDP(相比TCP操作更简洁)

send 发送方
- socket
- sendto(data,addr,......)//发送
- close

recv 接受方
- socket
- bind
- recvfrom
- close
任务
1.重写
    - 程序启动指定端口号 9000 8000
    - 程序创建两个子线程：send，recv
        - send
        - recv
        - 公用 fd还是各自持有