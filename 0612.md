# thread  线程

## 进程与线程
进程：程序的一次执行过程（动态），及在这个过程中获得的cpu,内存,文件,网络等资源
    一个程序运行的时候默认一个进程，也可以创建多个子进程，获得多个资源
    每个进程的资源都是独立的，有自己的cpu,内存等资源

线程：是进程中的最小执行单元，一个进程中默认有一个主线程
    根据需要可以创建多个子线程，同一个进程中的多个线程可以共享进程中的资源


## POSIX thread

POSIX 可移植 OS (interface) 接口扩展，一套由IEEE定义的技术规范
很多的操作系统遵循这 POSIX 规范：UNIX (IBM,Oracle，HP)，Linux，MAcOS,鸿蒙


## Pthread

- POSIX thread
- 0penMP
- 微软

相关API
- pthread_t             线程ID
- pthread_create()      创建一个新线程
- pthread_jain()        主线程等待join的线程执行结束
- pthread_self()        获得线程ID
- pthread_exit()        结束线程
