#openSSL

##MD5,SHA1,SHA256
- 单向，不可逆
- 数字指纹，摘要，唯一性
- 加密
- 密码绝大多数用SHA256+盐的方式存储

##Base64 编码和解码
- 二进制转为文本格式
- 文件增大，多出1\3
- 双向
- 加密
- 64种字符 a-z 26*2 0-9 +=

##加密与解密
- RSA 非对称密钥加密与解密
- 

##任务
1.

2.base64的使用场景


##示例
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <openssl/evp.h>
#include <openssl/bio.h>//bio：基本输入输出
#include <openssl/buffer.h>

//定义一个encode编码,有动态内存的分配
char *encode(char *str)
{
    BIO *b64 = BIO_new(BIO_f_base64());//创建了一个做base64编码的过滤器

    BIO *bio = BIO_new(BIO_s_mem());//source(s)为内存的bio

    bio = BIO_push(b64,bio);//给bio加上 base64 过滤器

    BIO_set_flags(bio,BIO_FLAGS_BASE64_NO_NL);//不换行

    BIO_write(bio,str,strlen(str));//写操作
    BIO_flush(bio);
    BUF_MEM *buf;//内存缓冲区
    BIO_get_mem_ptr(bio,&buf);

    char *data = malloc(buf->length+1);//动态分配的堆空间
    memcpy(data,buf->data,buf->length);
    data[buf->length] = '\0';
    BIO_free_all(bio);

    return data;
}
//解码
char *decode(char *data,int *len)
{
    BIO *b64 = BIO_new(BIO_f_base64());//创建一个过滤器

    int input_len = strlen(data);
    printf("input:%d\n",input_len);

    BIO *bio = BIO_new_mem_buf(data,input_len);
    bio = BIO_push(b64,bio);
    BIO_set_flags(bio,BIO_FLAGS_BASE64_NO_NL);
    
    char *buf = malloc(input_len);
    *len = BIO_read(bio,data,input_len);
    printf("output = %d",*len);
    BIO_free_all(bio);

    return buf;
}
//文件编码
void encode_file(char *in_file,char *out_file)
{
    BIO *in = BIO_new_file(in_file,"rb");//读

    BIO *out = BIO_new_file(out_file,"w");//写
//给输出流加入一个过滤器
    BIO *b64 = BIO_new(BIO_f_base64);
    out = BIO_push(b64,out);

    char buf[1024*4];
    int n;
    while (n = BIO_read(in,buf,sizeof(buf))>0);
    {
        bio_write(out,buf,n);
    }
    BIO_flush(out);
    BIO_free_all(in);
    bio_free_all(out);
    printf("ok\n");
}
//文件解码
void decode_file(char *in_file,char *out_file)
{
    BIO *in = BIO_new_file(in_file,"r");
    BIO *out = BIO_new_file(out_file,"wb");

    BIO *b64 = BIO_new(BIO_f_base64);
    in = BIO_push(b64,in);

    char buf[1024*4];
    int n;
    while (n = BIO_read(in,buf,sizeof(buf))>0);
    {
        BIO_write(out,buf,n);
    }
    BIO_flush(out);
    BIO_free_all(in);
    BIO_free_all(out);
    printf("ok\n");

}

//编码
// int main(int argc, char const *argv[])
// {
//     //文本的编码与解码
//     char *str = "hello BASE64";
//     char *base64=encode(str);//编码之后的内容
//     printf("%s\n",base64);

//     //文件的编码与解码
//     return 0;
// }

//解码
// int main(int argc, char const *argv[])
// {
//     char *data = "aGVsbG8gQkFTRTY0";

//     int len = 0;
//     char *text = decode(*data,&len);
//     printf("%s\n",text);
//     printf("%s\n",text,len);
//     free(text);
//     return 0;
// }

//文件编码
// int main(int argc, char const *argv[])
// {
//     encode_file("tux.png","tux.txt");

//     return 0;
// }

//文件解码
int main(int argc, char const *argv[])
{
    decode_file("tux.txt","tux.png");
    return 0;
}

