# C++ - 面向对象编程

## 对象和类

## 封装

## 继承

角色
- 父类,基类,超类
- 子类,派生类:获得父类中的属性和行为

特性
- 代码的重用，多个类的公共成员提取出来放在父类中
- 易于维护和扩展:修改父类

类型
- 单继承
- 多继承:存在副作用

属性
- 子类拥有父类中所有属性:私有(不能直接访问，可通过构造函数初始化),受保护的,共有的
- 子类中可以访问父类的 受保护的 公有的
- 受保护的属性子类可见

方法:
- 子类可以覆盖父类中的方法,重写 override,父类的方法是虚函数
- 仅限指针类型与对象类型不一致 父类的析构函数必须是虚的
## 多态

方法的多态，更具对象类型，参数类型，参数个数选择不同版本的方法执行，呈现不同的行为

- 重载:一个类内部，同名方法有多个版本，参数类型与参数个数不同，编译时选择不同方法执行
- 重写:在继承关系中，子类覆盖了父类的同名方法，更具对象的类型选择方法的不同版本

## 纯虚函数和抽象类

